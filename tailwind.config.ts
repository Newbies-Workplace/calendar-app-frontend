import { Config } from "tailwindcss/types";

const config: Config = {
	content: ["./src/**/*.{html,js,jsx,ts,tsx}"],
	theme: {
		extend: {
			colors: {},
		},
	},
	plugins: [],
};
export default config;
